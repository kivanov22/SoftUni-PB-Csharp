using System;

namespace Exam_Preparation
{
    class Program
    {
        static void Main(string[] args)
        {
            int students = int.Parse(Console.ReadLine());
            int tasks = int.Parse(Console.ReadLine());
            int trainerEneergy = int.Parse(Console.ReadLine());

            for (int i = 1; i < length; i++)
            {

            }

        }
    }
}
